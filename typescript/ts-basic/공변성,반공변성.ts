function axv(x: string): number {
  return 0;
}
type Bfsd = (x: string) => number | string;
let bdc: Bfsd = axv;

// function axv(x: string): number | string {
//   return 0;
// }
// type Bfsd = (x: string) => number;
// let bdc: Bfsd = axv;

// function axv(x: string | number): number {
//   return 0;
// }
// type Bfsd = (x: string) => number;
// let bdc: Bfsd = axv;

// function axv(x: string): number {
//   return 0;
// }
// type Bfsd = (x: string | number) => number;
// let bdc: Bfsd = axv;
